# without mod value 
print(pow(2, 3))

# with mod value 
print(pow(2, 3, 5))

# cases in pow function

# pos & pos -> int (pos)
print(pow(2, 3))

# pos & neg -> float (pos)
print(pow(2, -3))

# neg & pos -> int (neg)
print(pow(-2, 3))

# neg & neg -> float (neg)
print(pow(-2, -3))
